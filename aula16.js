function parimpar(n) {
    if (n % 2 === 0) {
        return 'par!';
    } else {
        return 'impar!';
    }
}

let res = parimpar(3); // Corrigido o nome da função para paimpar
console.log(res);
